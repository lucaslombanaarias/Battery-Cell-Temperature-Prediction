[pytest]
testpaths = tests
markers =
    slow: end-to-end pipeline test that trains a model
filterwarnings =
    # Known joblib<->NumPy 2.5 deprecation when (de)serializing the model; not our code.
    ignore:Setting the shape on a NumPy array has been deprecated:DeprecationWarning
